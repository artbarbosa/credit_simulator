import 'package:flutter/material.dart';

class CreditResultError extends StatelessWidget {
  const CreditResultError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
